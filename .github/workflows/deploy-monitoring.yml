name: Deploy Monitoring - Direct Access

on:
  workflow_dispatch:
    inputs:
      task:
        description: "Deploy or diagnose the monitoring stack with direct URLs"
        required: true
        default: deploy
        type: choice
        options:
          - deploy
          - diagnose

env:
  SCW_DEFAULT_REGION: fr-par
  SCW_DEFAULT_ZONE: fr-par-1
  CLUSTER_NAME: devops-factory-dev

concurrency:
  group: monitoring-${{ github.ref }}
  cancel-in-progress: false

jobs:
  monitoring:
    name: Monitoring direct access
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Resolve kubeconfig
        env:
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
          CLUSTER_ID_DEV: ${{ secrets.CLUSTER_ID_DEV }}
        run: |
          set -euo pipefail

          CLUSTER_ID="${CLUSTER_ID_DEV:-}"
          if [ -z "${CLUSTER_ID}" ]; then
            CLUSTER_ID="$(curl -fsS \
              -H "X-Auth-Token: ${SCW_SECRET_KEY}" \
              "https://api.scaleway.com/k8s/v1/regions/${SCW_DEFAULT_REGION}/clusters" \
              | jq -r --arg name "${CLUSTER_NAME}" '.clusters[] | select(.name == $name) | .id' \
              | head -n1)"
          fi

          if [ -z "${CLUSTER_ID}" ] || [ "${CLUSTER_ID}" = "null" ]; then
            echo "Cluster ${CLUSTER_NAME} introuvable. Lance d'abord Deploy Infrastructure."
            exit 1
          fi

          curl -fsS \
            -H "X-Auth-Token: ${SCW_SECRET_KEY}" \
            "https://api.scaleway.com/k8s/v1/regions/${SCW_DEFAULT_REGION}/clusters/${CLUSTER_ID}/kubeconfig?dl=1" \
            -o kubeconfig.yaml

          chmod 600 kubeconfig.yaml
          echo "KUBECONFIG=${PWD}/kubeconfig.yaml" >> "$GITHUB_ENV"

      - name: Run monitoring automation
        if: always()
        run: bash monitoring/deploy-monitoring.sh "${{ inputs.task }}"
