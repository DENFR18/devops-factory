apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-beta
  namespace: argocd
  labels:
    app.kubernetes.io/name: tenant-beta
    app.kubernetes.io/part-of: tenants
    tenant: beta
spec:
  project: default
  source:
    repoURL: https://github.com/DENFR18/devops-factory.git
    targetRevision: HEAD
    path: argocd/tenants/beta
    directory:
      exclude: values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
