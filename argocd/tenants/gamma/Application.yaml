apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-gamma-service
  namespace: argocd
  labels:
    app.kubernetes.io/name: tenant-gamma-service
    app.kubernetes.io/part-of: tenants
    tenant: gamma
spec:
  project: tenant-gamma
  source:
    repoURL: https://github.com/DENFR18/devops-factory.git
    targetRevision: HEAD
    path: charts/react
    helm:
      valueFiles:
        - ../../argocd/tenants/gamma/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tenant-gamma
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
